[project]
name = "nature"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pydantic",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
